\section{Table of Non-Descriptive Symbols}

Table~\ref{tab:symbols} lists non-descriptive symbols used in this work.

\begin{table}[h]
\capstart
\centering
\caption{Non-descriptive symbols.}
\label{tab:symbols}
\begingroup
\let\MyColumn=&
\catcode`\&=\catcode`\~
\let\oldtabular\tabular
% This hack of \begingroup \endgroup is devilish.
\def\SetMyRow{\begingroup\def\\{\unskip\hspace*{0.5em}\endgroup\\}}
\WideNarrow{
  \def&&{\MyColumn\SetMyRow}
  \def\tabular#1#2{\oldtabular{#1}}
}{
  \def&&{\MyColumn\MyColumn\SetMyRow}
  \def\tabular#1#2{\oldtabular{#2}}
}
\begin{tabular}{rl}{rcl}
\toprule
\hspace*{0.5em}\textbf{Symbol} && \textbf{Meaning} \\
\midrule
$\lambda$ && security parameter \\
$\scriptA,\scriptB,\scriptD$ && adversary, adversary, distinguisher \\
$\mu$ && message \\
$N,j$ && number of recipients, index \\
$i_\bot,i^\ast$ && cut-off index, traitor index \\
$Q,S,t$ && number of key pairs, set of non-traitors, traitor index \\
$C,\widehat{C},\widetilde{C}$ && circuit, garbled, obfuscated \\
$M_0,m_0$ && length of non-hardwired input ($\pk$), index \\
$L$ && label of garbled circuit \\
$D,\widehat{D}$ && database ($\pk$'s), processed \\
$M,m$ && length of database ($NM_0$), index \\
$k,\mathring{k}_x$ && PPRF key, punctured at~$x$ \\
\bottomrule
\end{tabular}
\endgroup
\end{table}
