\begin{proof}[\hyperanchor{pf:lower-bound}{Theorem~\ref{thm:lower-bound}}]
\luoji{Consider using the phrase ``blatantly insecure'' to indicate that the adversary algorithm is just decryption.}
\end{proof}
